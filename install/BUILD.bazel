# TODO: dylib and so condition on osx vs linux
cc_library(
    name = "opencv",
    srcs = glob(
      ["lib/*.dylib*"]),
    hdrs = glob([
      "include/**/*.hpp",
      "include/**/*.h",
    ]),
    includes = ["include/opencv4"],
    visibility = ["//visibility:public"], 
    linkstatic = 1,
)
